export type MenuOption = {
  id: string;
  menuItemId: string;
  title: string;
  titleDisplay: string;
  position: number;
  active: boolean;
  inside: boolean;
  outside: boolean;
  price: number;
  createdAt: string;
  updatedAt: string;
};
